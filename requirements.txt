pytest>=8,<9
# xlrd<2.0 required: xlrd 2.0 dropped .xls support; NY Fed ACM publishes legacy .xls
xlrd>=1.2,<2.0
# Phase C TradingView authenticated-candidate path; see
# docs/source_reviews/tradingview_authenticated_candidates.md
# Pinned to a specific commit of the rongardF fork (probed live 2026-05-11;
# symbol TVC:MOVE confirmed returning 5000-bar daily OHLCV back to 2005-12-15).
# If this fork becomes unavailable, the source review documents the failure-mode
# response: pause Phase C TradingView candidates, replace with a maintained
# tvdatafeed alternative or revisit the authenticated-candidate access path.
pandas>=2.2,<3
requests>=2.32,<3
tvdatafeed @ git+https://github.com/rongardF/tvdatafeed.git@e6f6aaa7de43
